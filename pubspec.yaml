name: not_a_portfolio
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.0

dependencies:
  animated_text_kit: ^4.2.3
  flutter:
    sdk: flutter
  flutter_animate: ^4.5.2
  flutter_svg: ^2.1.0
  google_fonts: ^6.2.1
  responsive_framework: ^1.5.1
  url_launcher: ^6.3.1
  visibility_detector: ^0.4.0+2
  web_smooth_scroll: ^2.0.0

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/
